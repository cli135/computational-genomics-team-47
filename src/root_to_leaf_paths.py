# Step 5. Root to leaf paths to find the most likely contaminant
def find_highest_weighted_root_to_leaf_path():
  """

  Given the hit_counts array and a parent map, we

  
  Keyword arguments:
  argument -- description
  Return: return_description
  """
  
def find_likely_contaminant(root_to_leaf_path):
  """
  Given the highest root to leaf path, computed in the above method,
  return the leaf of this path, which is the species identified as the contaminant.

  (More information in the Kraken paper about the Kraken algorithm)
  
  @return: leaf node of this root to leaf path
  """
  # TODO
  pass
  